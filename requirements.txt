# Core dependencies for PIHMMI
biopython>=1.79
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
matplotlib>=3.4.0
requests>=2.25.0
scikit-learn>=1.0.0
phytreeviz>=0.1.0

# Optional dependencies (install separately)
pyhmmer>=0.10.0  # Linux only, needed for HMM functionality
orthodb-py       # For OrthoDB access, requires special installation